
export enum MiddlewareStatuses {
    BadCookie = "bad cookie",
    InvalidId = "no user with such id",
    NoCookie = "no cookie",
    AnotherUserCookie = "another user cookie",
    AccessCookie = "access cookie"

}